# bib_extractor
This script tries to find the regions of an image which look like a template bib

There should be a folder which contains all the images from which bib regions need to be extracted. 

script should be run as 

python bib_extract.py <bib_template_folder> <image_folder> <threshold>

typical threshold is 0.8
